const config = {
    routes: {
        home: "/",
        products: "/products",
        productDetail: "/products/:id",
        notFound: "*",
        introduce: "/introduce",
        contact: "/contact",
        register: "/register",
        login: "/login",
    },
};

export default config;
